# 需求规格书：博彩决策助手（代号 lucky-tools）

> 本文档是完整实现规格，交给代码生成方（Codex）按此实现。
> 验收标准见同目录 `ACCEPTANCE.md`，实现必须让其中每一条通过。

## 1. 产品定位

一个**本地运行、单二进制、轻量化**的博彩数学决策工具。用户手动输入赔率和自己估计的胜率，工具用凯利公式等方法回答两个问题：**这注该不该下？该下多少？**

- 不联网、不抓取任何外部数据、不存储任何数据（MVP 无数据库）
- 用户群：懂基本概念的个人用户，中文界面
- 运行方式：双击可执行文件 → 自动监听 `http://127.0.0.1:8787` → 用户在浏览器（电脑或手机同局域网均可）打开使用

## 2. 技术栈与工程约束

| 项 | 要求 |
|---|---|
| 语言 | Rust（stable，edition 2021） |
| Web 框架 | axum + tokio |
| 序列化 | serde / serde_json |
| 静态资源 | rust-embed（或 include_dir）嵌入二进制 |
| 前端 | 原生 HTML/CSS/JavaScript，**禁止引入 node/npm 构建链，禁止 CDN 外链** |
| 数据库 | 无 |
| 其他依赖 | 尽量少，非必要不加 |

### 2.1 工程结构（Cargo workspace）

```
（仓库根目录）
├── Cargo.toml            # [workspace] members = ["crates/core", "crates/server"]
├── crates/
│   ├── core/             # 纯计算库：无 IO、无 async、无 serde 之外的依赖
│   │   └── src/lib.rs    # 所有公式实现 + 单元测试
│   └── server/           # 可执行程序：axum 路由 + 参数校验 + 静态资源
│       ├── src/main.rs
│       └── assets/       # index.html / app.js / style.css
├── REQUIREMENTS.md       # 本文件
└── ACCEPTANCE.md
```

**硬性要求：所有数学逻辑必须在 `core` crate 且可脱离 HTTP 层单元测试；`server` 只做 JSON 编解码、输入校验和错误包装。**

### 2.2 通用约定

- 所有概率、赔率、金额使用 `f64`
- 输入校验（在 server 层做，core 层用 debug_assert 或返回 Result 兜底）：
  - 概率 `p`：必须满足 `0 < p < 1`
  - 小数赔率 `odds`：必须满足 `odds > 1`
  - 本金 `bankroll`、注额 `stake`：必须 `> 0`
  - 组合凯利注数：`1 ≤ n ≤ 10`
- API 统一前缀 `/api/`，全部 `POST`，请求响应均为 JSON
- 错误响应统一格式，HTTP 422：`{"error": "<中文错误描述>"}`，如 `{"error": "胜率必须在 0 和 1 之间"}`
- 服务启动后在终端打印一行中文提示：`博彩决策助手已启动：http://127.0.0.1:8787`
- 端口可用环境变量 `PORT` 覆盖，默认 8787

## 3. 功能规格（5 个计算器）

### 3.1 凯利公式计算器 — `POST /api/kelly`

**目的**：给定赔率和自估胜率，算出最优仓位；没有优势时明确说"不该下"。

**公式**：
- `b = odds − 1`（净赔率）
- `edge = b·p − (1−p)`（每 1 元注的期望净收益）
- `f* = edge / b`（全凯利仓位）
- 建议注额 = `bankroll × max(f*, 0) × fraction`
- `EV = edge × 建议注额`

**请求**：
```json
{ "odds": 2.5, "p": 0.5, "bankroll": 1000.0, "fraction": 0.5 }
```
`fraction` 为凯利分数，取值 1.0 / 0.5 / 0.25（前端下拉），API 层接受 (0, 1] 内任意值。

**响应**：
```json
{
  "edge": 0.25,
  "kelly_fraction": 0.166667,
  "stake": 83.33,
  "ev": 20.83,
  "should_bet": true,
  "verdict": "有优势，建议下注 83.33（半凯利）"
}
```

**关键行为（灵魂需求）**：当 `f* ≤ 0` 时，`should_bet = false`，`stake = 0`，`verdict` 明确写「无优势，不该下」。任何情况下不得输出负注额。

### 3.2 组合凯利 — `POST /api/kelly/multi`

**目的**：同时看好 n 场**相互独立**的比赛时，逐场套单注凯利会导致总仓位超配；本接口对 n 场做联合最优。

**算法**（必须按此实现，n ≤ 10 所以可枚举）：
1. 枚举全部 `2^n` 个胜负组合，组合 `S` 的概率为 `∏(i∈S) pᵢ × ∏(i∉S) (1−pᵢ)`
2. 目标函数：`E[ln W]`，其中 `W = 1 + Σᵢ fᵢ·(bᵢ·xᵢ − (1−xᵢ))`，`xᵢ ∈ {0,1}` 表示第 i 场是否赢
3. 在约束 `fᵢ ≥ 0`、`Σfᵢ ≤ 0.999`（防止 ln 于 0 处爆炸）下最大化，用投影梯度上升或坐标下降，迭代至收敛（梯度范数 < 1e-9 或 10000 步）
4. 求得 `fᵢ*` 后，各场建议注额 = `bankroll × fᵢ* × fraction`

**请求**：
```json
{
  "bets": [ { "odds": 2.5, "p": 0.5 }, { "odds": 2.1, "p": 0.55 } ],
  "bankroll": 1000.0,
  "fraction": 1.0
}
```

**响应**：
```json
{
  "allocations": [
    { "index": 0, "kelly_fraction": 0.152, "stake": 152.0, "naive_kelly": 0.166667 },
    { "index": 1, "kelly_fraction": 0.135, "stake": 135.0, "naive_kelly": 0.140909 }
  ],
  "total_fraction": 0.287,
  "total_stake": 287.0,
  "note": "已做联合优化，总仓位低于逐场单注凯利之和"
}
```
（`naive_kelly` 为该场单独套 3.1 公式的结果，用于前端对比展示；上面数字仅示意格式，以算法实际收敛值为准。）

**必须满足的数学性质**（core 层写成测试）：
- n = 1 时结果与 3.1 单注凯利一致（容差 1e-4）
- 每场 `fᵢ*` ≤ 该场 naive kelly；`Σfᵢ*` ≤ Σ naive kelly
- 某场 edge ≤ 0 时该场 `fᵢ* = 0`

### 3.3 赔率格式互转 — `POST /api/odds/convert`

**目的**：六种赔率格式互转 + 隐含概率。内部先归一到小数盘 `d`，再转出全部格式。

| 格式 | key | 由小数盘 d 转出 | 反向（输入该格式 v 时求 d） |
|---|---|---|---|
| 小数盘 | `decimal` | `d` | `d = v`（要求 v > 1） |
| 美式 | `american` | d ≥ 2 → `+100(d−1)`；d < 2 → `−100/(d−1)` | v > 0 → `1 + v/100`；v < 0 → `1 + 100/(−v)`（要求 \|v\| ≥ 100） |
| 香港盘 | `hongkong` | `d − 1` | `d = v + 1`（要求 v > 0） |
| 马来盘 | `malay` | d ≤ 2 → `d − 1`；d > 2 → `−1/(d−1)` | v > 0 → `v + 1`（要求 v ≤ 1）；v < 0 → `1 − 1/v` |
| 印尼盘 | `indonesian` | d ≥ 2 → `d − 1`；d < 2 → `−1/(d−1)` | v > 0 → `v + 1`（要求 v ≥ 1）；v < 0 → `1 − 1/v` |
| 分数盘 | `fractional` | `d − 1` 化为最简分数（分母 ≤ 100 的最佳有理近似），字符串如 `"3/2"` | `d = 1 + 分子/分母` |
| 隐含概率 | `implied_probability` | `1/d` | —（仅输出） |

**请求**：`{ "value": 2.5, "format": "decimal" }`（`fractional` 格式时 value 传字符串 `"3/2"`，其余传数字；实现上 value 可用 untagged enum 或统一收字符串再解析，任选，但两种都要能收）

**响应**：包含上表全部 7 个 key：
```json
{
  "decimal": 2.5, "american": 150.0, "hongkong": 1.5,
  "malay": -0.6667, "indonesian": 1.5,
  "fractional": "3/2", "implied_probability": 0.4
}
```
边界：d = 2 时美式输出 `+100`、马来 `1.0`、印尼 `1.0`。

### 3.4 去水计算 — `POST /api/vig`

**目的**：从庄家一场比赛全部结果的赔率里剥掉抽水，得到市场真实隐含概率，用户拿它和自己的估计对比。

**公式**（乘法去水）：
- 每个结果隐含概率 `qᵢ = 1/oᵢ`
- 抽水率（overround）`v = Σqᵢ − 1`
- 去水真实概率 `pᵢ = qᵢ / Σqⱼ`
- 公平赔率 `fairᵢ = 1/pᵢ`

**请求**：`{ "odds": [2.10, 3.40, 3.60], "labels": ["主胜", "平局", "客胜"] }`
（`odds` 长度 2 或 3；`labels` 可选，缺省用「结果1/结果2/结果3」）

**响应**：
```json
{
  "overround": 0.048086,
  "overround_percent": "4.81%",
  "outcomes": [
    { "label": "主胜", "implied": 0.476190, "true_probability": 0.454343, "fair_odds": 2.2010 },
    { "label": "平局", "implied": 0.294118, "true_probability": 0.280624, "fair_odds": 3.5635 },
    { "label": "客胜", "implied": 0.277778, "true_probability": 0.265034, "fair_odds": 3.7731 }
  ]
}
```
若 `Σqᵢ ≤ 1`（出现套利盘），照常计算并在响应加 `"note": "赔率组合存在套利空间"`。

### 3.5 EV 期望值计算器 — `POST /api/ev`

**目的**：任意一注的期望值、期望回报率和波动。

**公式**：
- `EV = p·(odds−1)·stake − (1−p)·stake`
- `roi = EV / stake`
- 单注盈亏标准差：赢得 `(odds−1)·stake`、输掉 `−stake`，`std = stake·odds·sqrt(p(1−p))`
- 长期展望：`n` 注（默认 100，请求可选传 `n`）后期望累计盈亏 `n·EV`，累计标准差 `std·sqrt(n)`

**请求**：`{ "odds": 2.5, "p": 0.45, "stake": 100.0, "n": 100 }`

**响应**：
```json
{
  "ev": 12.5, "roi": 0.125, "std_dev": 124.37,
  "long_run": { "n": 100, "expected_profit": 1250.0, "std_dev": 1243.73 },
  "positive_ev": true
}
```

## 4. 前端规格

单页应用（一个 `index.html`），5 个 Tab 依次为：**凯利计算 / 组合凯利 / 赔率转换 / 去水计算 / 期望值**。

- 全部界面文案为简体中文；数字输入用 `<input type="number">`，移动端弹数字键盘
- 响应式：viewport ≤ 480px（手机竖屏）时布局不横向溢出、按钮可点
- 结果展示：`should_bet/positive_ev` 为真时结果卡片绿色系并显示「✅ 有优势，可考虑下注」；为假时红色系「⛔ 无优势，不该下」
- 凯利 Tab 的凯利分数用下拉框：全凯利(1)/半凯利(0.5，默认)/四分之一凯利(0.25)，旁边灰字提示「分数凯利可大幅降低破产风险」
- 组合凯利 Tab 支持动态增删比赛行（2~10 行）
- 调用后端出错（422）时把 `error` 字段的中文消息展示在表单下方，不弹 alert
- 页脚固定免责声明：**「凯利公式的一切结论都建立在你对胜率的估计准确之上。本工具仅为数学计算，不构成任何投注建议。请设定止损、量力而行。」**
- 不要求美术设计，整洁可用即可；深色或浅色主题任选其一做好

## 5. 明确不做（防止范围蔓延）

- 不接任何赔率 API、不爬虫、不联网
- 无用户系统、无数据库、无下注记录（列入 v2）
- 无套利扫描、无 CLV 追踪（列入 v3）
- 不做 i18n 框架，中文硬编码即可

## 6. Roadmap（仅供了解，本期不实现）

- v2：SQLite 下注流水、盈亏曲线、日/周止损提醒
- v3：赔率数据源抽象层 + The Odds API 接入、跨庄套利扫描、CLV 追踪
